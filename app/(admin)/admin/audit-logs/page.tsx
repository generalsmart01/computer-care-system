import{connectDB}from"@/lib/db";import{AuditLog}from"@/models/AuditLog";export default async function Page(){await connectDB();const logs=await AuditLog.find().sort({createdAt:-1}).limit(100).lean();return <><h1>Audit logs</h1>{logs.length?<table><caption className="sr-only">Most recent audit activity</caption><thead><tr><th scope="col">Time</th><th scope="col">Action</th><th scope="col">Entity</th></tr></thead><tbody>{logs.map((x:any)=><tr key={String(x._id)}><td>{new Date(x.createdAt).toLocaleString()}</td><td>{x.action}</td><td>{x.entityType} {String(x.entityId)}</td></tr>)}</tbody></table>:<p className="card">No audit activity has been recorded.</p>}</>}
