import Link from"next/link";import{getSession}from"@/lib/auth";import{dashboardForRole}from"@/lib/role-navigation";
export default async function Page(){const user=await getSession();return <main className="section container"><section className="card"><p className="status">403</p><h1>Access denied</h1><p>Your account does not have permission to view that area.</p><p>{user?<Link className="btn" href={dashboardForRole(user.role)}>Return to your dashboard</Link>:<Link className="btn" href="/login">Log in</Link>} <Link className="btn secondary" href="/">Go home</Link></p></section></main>}
