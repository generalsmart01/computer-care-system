import{describe,expect,it}from"vitest";import{diagnosisDraftSchema,diagnosisRevisionRequestSchema,diagnosisSubmissionSchema,formatParts}from"@/features/diagnoses/diagnosis.validation";
const complete={observedSymptoms:"Won't boot, clicking drive",faultCategory:"Storage",faultDescription:"The drive has failed diagnostics.",recommendedAction:"Replace the drive and restore data.",requiredParts:"SSD | 1 | 80.50",internalNotes:"SMART errors observed",customerSummary:"The storage drive needs replacement."};
describe("diagnosis validation",()=>{it("allows incomplete drafts",()=>{expect(diagnosisDraftSchema.safeParse({faultCategory:""}).success).toBe(true)});it("normalizes submitted symptoms and parts",()=>{expect(diagnosisSubmissionSchema.parse(complete)).toMatchObject({observedSymptoms:["Won't boot","clicking drive"],requiredParts:[{name:"SSD",quantity:1,estimatedUnitPrice:80.5}]})});it("requires complete customer-safe submission fields",()=>{expect(diagnosisSubmissionSchema.safeParse({}).success).toBe(false)});it("requires a meaningful bounded revision reason",()=>{expect(diagnosisRevisionRequestSchema.parse({reason:"  Recheck the power diagnosis  "}).reason).toBe("Recheck the power diagnosis");expect(diagnosisRevisionRequestSchema.safeParse({reason:"no"}).success).toBe(false)});it("formats parts for draft editing",()=>{expect(formatParts([{name:"Fan",quantity:2,estimatedUnitPrice:12}])).toBe("Fan | 2 | 12")})});
