import{describe,expect,it}from"vitest";import{FAQS,PROCESS_STEPS,TRUST_REASONS}from"@/lib/public-content";
describe("public website content",()=>{it("provides the complete four-stage customer process",()=>{expect(PROCESS_STEPS).toHaveLength(4);expect(PROCESS_STEPS.every(item=>item.title&&item.text)).toBe(true)});it("provides distinct trust reasons",()=>{expect(TRUST_REASONS.length).toBeGreaterThanOrEqual(4);expect(new Set(TRUST_REASONS.map(item=>item.title)).size).toBe(TRUST_REASONS.length)});it("provides useful, unique FAQ entries",()=>{expect(FAQS.length).toBeGreaterThanOrEqual(6);expect(new Set(FAQS.map(item=>item.question)).size).toBe(FAQS.length);expect(FAQS.every(item=>item.answer.length>40)).toBe(true)})});
