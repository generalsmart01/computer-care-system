import{describe,expect,it}from"vitest";import{canAcceptAssignment}from"@/features/assignments/assignment.rules";
describe("assignment acceptance",()=>{it("allows an active unaccepted assignment while the booking is assigned",()=>{expect(canAcceptAssignment("ACTIVE",undefined,"ASSIGNED")).toBe(true)});it("rejects repeated, historical, and late acceptance",()=>{expect(canAcceptAssignment("ACTIVE",new Date(),"ASSIGNED")).toBe(false);expect(canAcceptAssignment("REASSIGNED",undefined,"ASSIGNED")).toBe(false);expect(canAcceptAssignment("ACTIVE",undefined,"DIAGNOSING")).toBe(false)})});
