/** @vitest-environment jsdom */
import React from"react";import{cleanup,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it}from"vitest";import{PageSkeleton,PageState}from"@/components/ui/page-state";
afterEach(cleanup);describe("page states",()=>{it("presents a useful recovery action",()=>{render(<PageState code="404" title="Record not found" message="The record is unavailable." actionHref="/dashboard" actionLabel="Return"/>);expect(screen.getByRole("heading",{name:"Record not found"})).toBeTruthy();expect(screen.getByRole("link",{name:"Return"}).getAttribute("href")).toBe("/dashboard")});it("announces route loading",()=>{const{container}=render(<PageSkeleton/>);expect(container.querySelector('[aria-busy="true"]')).toBeTruthy();expect(screen.getByText("Loading page")).toBeTruthy()})});
