import{describe,expect,it}from"vitest";import{adminUserQuerySchema,buildAdminUserFilter,canChangeUserStatus,escapeUserSearch}from"@/features/admin/user-list.validation";
describe("admin user management",()=>{it("escapes search and builds role/status filters",()=>{expect(escapeUserSearch("Alex.*")).toBe("Alex\\.\\*");const query=adminUserQuerySchema.parse({search:"Alex.*",role:"CUSTOMER",status:"ACTIVE"}),filter=buildAdminUserFilter(query)as any;expect(filter).toMatchObject({role:"CUSTOMER",status:"ACTIVE"});expect(filter.$or).toEqual([{firstName:{$regex:"Alex\\.\\*",$options:"i"}},{lastName:{$regex:"Alex\\.\\*",$options:"i"}},{email:{$regex:"Alex\\.\\*",$options:"i"}},{phone:{$regex:"Alex\\.\\*",$options:"i"}}])});it("validates pagination and supported filters",()=>{expect(adminUserQuerySchema.parse({page:"2",limit:"50"})).toMatchObject({page:2,limit:50});expect(adminUserQuerySchema.safeParse({role:"OWNER"}).success).toBe(false);expect(adminUserQuerySchema.safeParse({limit:1000}).success).toBe(false)});it("prevents administrators changing their own status",()=>{expect(canChangeUserStatus("same","same","ACTIVE","SUSPENDED")).toMatchObject({allowed:false})});it("allows only active and suspended state changes",()=>{expect(canChangeUserStatus("admin","user","ACTIVE","SUSPENDED")).toMatchObject({allowed:true});expect(canChangeUserStatus("admin","user","DEACTIVATED","ACTIVE")).toMatchObject({allowed:false});expect(canChangeUserStatus("admin","user","ACTIVE","ACTIVE")).toMatchObject({allowed:false})})});
