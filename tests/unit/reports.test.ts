import{describe,expect,it}from"vitest";import{ACTIVE_REPAIR_STATUSES,buildReportDateMatch,reportQuerySchema}from"@/features/reports/report.validation";
describe("admin report filters",()=>{it("builds inclusive UTC date boundaries",()=>{const match=buildReportDateMatch(reportQuerySchema.parse({dateFrom:"2026-08-01",dateTo:"2026-08-31"}));expect(match).toEqual({createdAt:{$gte:new Date("2026-08-01T00:00:00.000Z"),$lte:new Date("2026-08-31T23:59:59.999Z")}})});it("rejects reversed and malformed ranges",()=>{expect(reportQuerySchema.safeParse({dateFrom:"2026-09-01",dateTo:"2026-08-01"}).success).toBe(false);expect(reportQuerySchema.safeParse({dateFrom:"08/01/2026"}).success).toBe(false)});it("allows an unbounded report",()=>{expect(buildReportDateMatch(reportQuerySchema.parse({}))).toEqual({})});it("defines active repair stages without terminal states",()=>{expect(ACTIVE_REPAIR_STATUSES).toContain("REPAIRING");expect(ACTIVE_REPAIR_STATUSES).toContain("READY_FOR_COLLECTION");expect(ACTIVE_REPAIR_STATUSES).not.toContain("COMPLETED");expect(ACTIVE_REPAIR_STATUSES).not.toContain("CANCELLED")})});
