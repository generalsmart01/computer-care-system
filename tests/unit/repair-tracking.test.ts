import{describe,expect,it}from"vitest";import{formatPartsUsed,formatTrackingLines,repairTrackingSchema}from"@/features/repairs/repair-tracking.validation";
describe("repair tracking validation",()=>{it("parses progressive work, test, and part rows",()=>{const result=repairTrackingSchema.parse({workPerformed:"Cleaned fan\nReplaced thermal paste",partsUsed:"Thermal paste | 1\nCooling fan | 2",testResults:"Boot test passed\nTemperature stable",finalNotes:"Repair progressing",warrantyDays:"90"});expect(result).toMatchObject({workPerformed:["Cleaned fan","Replaced thermal paste"],partsUsed:[{name:"Thermal paste",quantity:1},{name:"Cooling fan",quantity:2}],testResults:["Boot test passed","Temperature stable"],warrantyDays:90})});it("allows an incomplete repair draft",()=>{expect(repairTrackingSchema.parse({workPerformed:"",partsUsed:"",testResults:"",finalNotes:"",warrantyDays:""})).toEqual({workPerformed:[],partsUsed:[],testResults:[],finalNotes:"",warrantyDays:0})});it("rejects invalid part quantities and malformed rows",()=>{expect(()=>repairTrackingSchema.parse({partsUsed:"Fan | 0"})).toThrow();expect(()=>repairTrackingSchema.parse({partsUsed:"Fan"})).toThrow();expect(()=>repairTrackingSchema.parse({partsUsed:"Fan | 1 | extra"})).toThrow()});it("formats a persisted draft for editing",()=>{expect(formatTrackingLines(["Cleaned fan","Passed boot test"])).toBe("Cleaned fan\nPassed boot test");expect(formatPartsUsed([{name:"Fan",quantity:2}])).toBe("Fan | 2")})});
