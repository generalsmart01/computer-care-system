import{afterEach,beforeEach,describe,expect,it,vi}from"vitest";import{futureDateSchema,moneySchema,paginationSchema}from"@/lib/validators";import{calculateQuotation}from"@/features/quotations/calculation";
describe("date and price boundaries",()=>{beforeEach(()=>vi.useFakeTimers().setSystemTime(new Date("2026-08-13T12:00:00Z")));afterEach(()=>vi.useRealTimers());it("accepts today and rejects a past appointment date",()=>{expect(futureDateSchema.safeParse("2026-08-13").success).toBe(true);expect(futureDateSchema.safeParse("2026-08-12").success).toBe(false)});it("accepts zero money but rejects negative, excessive, and non-finite values",()=>{expect(moneySchema.parse("0")).toBe(0);for(const value of[-0.01,10_000_001,Infinity,NaN])expect(moneySchema.safeParse(value).success).toBe(false)});it("rounds line amounts and totals to currency precision",()=>{expect(calculateQuotation({items:[{description:"Labour",type:"LABOUR",quantity:3,unitPrice:.335}],discount:.01})).toMatchObject({subtotal:1.01,total:1})});it("bounds pagination to one through one hundred rows",()=>{expect(paginationSchema.parse({page:"2",limit:"100"})).toEqual({page:2,limit:100});expect(paginationSchema.safeParse({page:0,limit:20}).success).toBe(false);expect(paginationSchema.safeParse({page:1,limit:101}).success).toBe(false)})});
