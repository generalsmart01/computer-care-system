"use client";import{useActionState}from"react";import{startDiagnosisAction,type DiagnosisActionState}from"@/features/diagnoses/diagnosis.actions";export function StartDiagnosisForm({bookingId}:{bookingId:string}){const[state,action,pending]=useActionState(startDiagnosisAction.bind(null,bookingId),{} as DiagnosisActionState);return <form action={action} className="card"><h2>Begin diagnosis</h2><p>Starting records the workflow transition and opens the diagnosis worksheet.</p>{state.error&&<p className="error" role="alert">{state.error}</p>}<button className="btn" disabled={pending}>{pending?"Starting…":"Start diagnosis"}</button></form>}
