import"server-only";import{connectDB}from"@/lib/db";import{Booking}from"@/models/Booking";import{Review}from"@/models/Review";import{BusinessRuleError,ConflictError}from"@/lib/errors";import{reviewSchema}from"./review.validation";export{reviewSchema}from"./review.validation";
export async function createReview(customerId:string,input:unknown){const data=reviewSchema.parse(input);await connectDB();if(!await Booking.exists({_id:data.bookingId,customerId,status:"COMPLETED"}))throw new BusinessRuleError("Only your completed bookings can be reviewed");try{return await Review.create({...data,customerId})}catch(error){if(error&&typeof error==="object"&&"code"in error&&(error as{code?:number}).code===11000)throw new ConflictError("This booking has already been reviewed");throw error}}
