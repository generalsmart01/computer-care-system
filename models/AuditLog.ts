import mongoose,{Schema} from "mongoose";const schema=new Schema({actorId:{type:Schema.Types.ObjectId,ref:"User",required:true},action:{type:String,required:true},entityType:{type:String,required:true},entityId:{type:Schema.Types.ObjectId,required:true},summary:{type:Schema.Types.Mixed},ipAddress:String},{timestamps:{createdAt:true,updatedAt:false}});schema.index({entityType:1,entityId:1});schema.index({createdAt:-1});export const AuditLog:mongoose.Model<any>=mongoose.models.AuditLog||mongoose.model("AuditLog",schema);
