import mongoose,{Schema} from "mongoose";const schema=new Schema({userId:{type:Schema.Types.ObjectId,ref:"User",required:true},title:{type:String,required:true},message:{type:String,required:true},type:{type:String,required:true},readAt:Date,bookingId:{type:Schema.Types.ObjectId,ref:"Booking"}},{timestamps:true});schema.index({userId:1,createdAt:-1});schema.index({userId:1,readAt:1,createdAt:-1});export const Notification:mongoose.Model<any>=mongoose.models.Notification||mongoose.model("Notification",schema);
