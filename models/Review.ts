import mongoose,{Schema} from "mongoose";const schema=new Schema({bookingId:{type:Schema.Types.ObjectId,ref:"Booking",required:true,unique:true},customerId:{type:Schema.Types.ObjectId,ref:"User",required:true},rating:{type:Number,required:true,min:1,max:5},comment:{type:String,trim:true,maxlength:1000},isHidden:{type:Boolean,default:false}},{timestamps:true});schema.index({customerId:1,createdAt:-1});export const Review:mongoose.Model<any>=mongoose.models.Review||mongoose.model("Review",schema);
